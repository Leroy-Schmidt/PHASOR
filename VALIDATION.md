# PHASOR — Validation log

Gate log per DESIGN.md §6. Automated gates: `node --test` summary pasted verbatim.
Human gates: dated checklist entry signed by the Operator.

| Date | Commit | Gate | Result | Numbers / notes |
|---|---|---|---|---|
| 2026-06-12 | aff51fd | G0.1 (auto) | PASS | `node --test`: 18 pass / 0 fail. Painter: overlap order, shared-face split 32/32 exact, 3-box sandwich vs brute-force reference exact. Presets: wall1d 84 cells (eps 28 / brick 40 / plaster 16, counts exact vs tick intervals); corner2d corner cell = eps (insulation wraps), grading h(corner) < 0.5·h(max). |
| 2026-06-12 | 264aa45 | G0.2 (human) | PASS | Operator sign-off: corner2d layer order/thicknesses correct (eps 160 / brick 240 / plaster 15, exterior → interior; eps wraps the outer corner), grading visibly refines toward the interior corner. Caveat noted: mesh density along the outer→inner corner diagonal is relatively low — to be re-checked once M1's solver can measure it via convergence / f_Rsi gates (see BACKLOG.md). |
| 2026-06-13 | 2e06e48 | G1.1 (auto) | PASS | wall1d 21×2×2 cells, CG 24 it (tol 1e-12): max interface-temperature error **2.17e-13 K** vs series-resistance oracle (gate < 1e-6 K). Oracle U = 0.195553 W/m²K; interfaces 9.0860 / 18.9196 / 19.6571 / 19.7204 °C. Solver-precision-limited, as the gate demands. |
| 2026-06-13 | 2e06e48 | G1.2 (auto) | PASS | 2D Laplace (unit square, T = sin(πx) top): max-norm errors h = 1/8, 1/16, 1/32 → 4.458e-3, 1.117e-3, 2.785e-4; ratios **3.992, 4.011** (gate [3.2, 4.8]) — clean O(h²). |
| 2026-06-13 | 2e06e48 | G1.3 (auto) | PASS | Consistent Robin-face fluxes. wall1d: Φ_ext +2.1511e-2 W, Φ_int −2.1511e-2 W, imbalance **6.8e-13**·throughput; corner2d: ±5.3768 W, imbalance **1.1e-14**·throughput (gate < 1e-6). Cross-check: wall1d Φ_ext = U·ΔT·A to < 1e-6 relative. |
| 2026-06-13 | 2e06e48 | G1.4 (auto) | PASS | corner2d 31×31×7: max x↔y asymmetry 5.47e-13 K on 10.715 K range = **5.1e-14 relative** (gate < 1e-9); grid ticks xs ≡ ys exactly. |
| 2026-06-13 | 2e06e48 | M1 `node --test` | — | **tests 45, pass 45, fail 0, skipped 0, todo 0** (duration 1.5 s). Covers G1.1–G1.4 plus FEM element-matrix vs Gauss-quadrature verification (caught a FACE_P transcription bug: M_f[3][3] typed 1 instead of 4 — indefinite Robin matrix, CG breakdown; oracle did its job), BC classification, Dirichlet elimination, operator symmetry, CG and physics units. Readouts (corner2d): f_Rsi(R_si=0.25) = 0.897, θ_si,min = 18.87 °C, ψ_e = **−0.0646 W/(m·K) external dims**, L2D = 0.4888, U = 0.1956. f_Rsi monotone in eps thickness: 0.04/0.08/0.16/0.24 m → 0.747/0.832/0.897/0.924. |
| 2026-06-13 | 2e06e48 | G1.5 (human) | PASS | Operator sign-off: corner2d isotherms bend around the corner (textbook Wärmebrücken shape); coldest interior-surface point is at the re-entrant inner corner; f_Rsi rises monotonically under the insulation-thickness slider. Note for the G0.2 caveat: G1.2 O(h²) came out clean, no diagonal-grading action needed yet (BACKLOG stands). **M1 complete — all gates G1.1–G1.5 green; tagged m1-pass.** |
| 2026-06-14 | _working tree_ | G2.1 (auto) | PASS | Semi-infinite concrete slab, prescribed surface T̂=1, far end (12δ) Dirichlet T̂=0, adiabatic lateral. Coarse cpδ=10 / fine cpδ=20. **Both diurnal and annual ω**: max-amplitude-error ratio coarse/fine ∈ [3.2, 4.8] (clean O(h²)); on the fine mesh over 0.5δ≤x≤2δ, |T̂| vs A·e^{−x/δ} and lag vs x/δ both **< 1 %**. (Domain at 12δ so the far-BC truncation floor is e^{−24}, leaving pure discretization error.) |
| 2026-06-14 | _working tree_ | G2.2 (semi-auto) | PASS | Default wall1d (eps0.16/brick0.24/plaster0.015), fine x-mesh (maxH 2.5 mm), exterior driven at unit amplitude, interior air constant. 3D FEM inner-surface phasor vs independent ISO-13786 Python oracle (`tools/iso13786_oracle.py`): **annual** \|T̂_is/T̂_e\| FEM 2.541522e-2 vs oracle 2.541521e-2 (4e-7), arg −1.596° both, lag 1.619 d; **diurnal** FEM 2.181271e-3 vs oracle 2.180974e-3 (1.4e-4), arg −179.66° vs −179.651°, lag 11.977 h. Both amplitude **and** phase within 0.5 %. Certifies the e^{+iωt} sign convention end-to-end. |
| 2026-06-14 | _working tree_ | G2.3 (auto) | PASS | 9³-cell (10³ = 1000-node) concrete box, all-Robin, diurnal ω: COCG (Jacobi, complex diagonal) vs dense complex-LU oracle (operator probed by unit vectors, then Gaussian elimination w/ partial pivoting) — relative difference **< 1e-8**. |
| 2026-06-14 | _working tree_ | G2.4 (auto) | PASS (auto) | Inward path through wall1d (diurnal): time lag τ=−arg(T̂)/ω **strictly increases** with depth at every active node — the wall lags the weather, never anticipates it (the classic conjugation/sign bug would fail here). Human half: see scrubber note below; awaiting Operator sign-off. |
| 2026-06-14 | _working tree_ | G2.5 (auto) | PASS | 48³ concrete box, one frequency (diurnal), COCG to relRes < 1e-8 in 101 iterations: **~8 s isolated** (well under the 10 s budget; ~4 s inside the parallel `node --test`). Matvec is matrix-free with deduplicated element matrices (uniform box → 1 type), ω-prescaled capacity, gathered nodal values, all-free fast path. |
| 2026-06-14 | _working tree_ | M2 `node --test` | — | **tests 68, pass 68, fail 0, skipped 0, todo 0**. Adds G2.1–G2.5 plus primitives: elementC (consistent mass) vs Gauss quadrature; applyAComplex complex-**symmetry** ⟨Ax,y⟩=⟨x,Ay⟩ in the unconjugated form and ω=0 reduction to the M1 real operator; assembleHarmonicLoad; COCG (with/without imaginary preconditioner, non-convergence flag) and dense complex-LU; penetrationDepth (DESIGN §2.3 table), semiInfinite, climatePhasor (minimum at the stated offset), amplitude/timeLag, phasorEval. Complex fields stored as separate re/im Float64Arrays (CLAUDE.md M2). |
| 2026-06-14 | _working tree_ | M2 UI (viz2d) | — | Field selector T(t) \| amplitude \| phase-lag + frequency selector + day/year **time scrubber** wired (3 new controls, Yuki's rule). Verified in preview: wall1d solve returns mean + annual + diurnal fields; amplitude annual interior 0.254 K (=0.0254·10 K) / exterior ~9.9 K, diurnal interior 0.011 K / exterior ~4.95 K — matches G2.2. Scrubbing a day: exterior surface swings −4.8…+3.8 °C while the interior stays ~19.47 °C (wall damps the diurnal wave to ~0.01 K). Field evaluation is pure superposition — never re-solves (DESIGN §3.5). |
| 2026-06-14 | _working tree_ | G2.4 (human) | PASS | Operator sign-off: reviewed amplitude/phase fields, the day/year scrubber, and the new line-probe profile across `wall1d` and `soil_rod`. Confirmed the exterior breathes while the interior lags and damps; the `soil_rod` line probe shows the textbook exponentially-damped wave (envelope `e^{−x/δ}`, lag growing with depth) with no anticipation of the weather. Validated the wall amplitude physics directly (the interior-BC resistance divider vs the adiabatic near-constant case). |
| 2026-06-14 | _working tree_ | **off-script** `soil_rod` preset | NOTE | Added a teaching/validation preset **not in DESIGN §5.1** (Operator decision 2026-06-14): a 10 m soil column, exterior climate on top, deep far-field Dirichlet at T_mean, adiabatic sides — the 1-D semi-infinite solid made visible, and the sanity sibling of the M3 `basement`. Surfaced & fixed a latent bug: **solveHarmonic now imposes the far-field harmonic convention T̂_k=0** (DESIGN §2.2) by default (`dirichlet:'zero'`), independent of the steady Dirichlet value (=T_mean); `dirichlet:'value'` retained for the G2.1 surface drive. Verified live: annual amplitude 8.8 K just under the surface, decaying e^{−d/δ} (ratio 0.702/m vs theory 0.703), still 3.4 K at 3 m, 0.57 K at 8 m — the annual wave penetrates metres, exactly as δ_soil≈2.84 m predicts. 2 new tests (far-field T̂=0 regression; e^{−d/δ} decay + lag-with-depth). `node --test`: **70/70**. |
| 2026-06-14 | _working tree_ | line-probe plot (M4 pulled forward) | NOTE | New `src/lineprobe.mjs`: a 1-D profile panel — T vs distance along the geometry's primary axis with numbered axes, the static mean T̄(x), the selected-frequency envelope T̄±\|T̂\|, and the live single-frequency wave T̄+Re[T̂ e^{iωt}] that sweeps with the scrubber. The view that matches the "exponentially-damped wobbling sine" intuition; the instrument for the M3 basement phase-lag gate. Heatmap + line probe now live in **foldable panels** (verified: folding one reallocates the column to the other). Pure helpers `probeAxis`/`lineIndices` unit-tested. |
| 2026-06-14 | _working tree_ | **M2 complete** | PASS | All M2 gates green (G2.1–G2.5; G2.4 human signed off above). `node --test`: **tests 72, pass 72, fail 0, skipped 0, todo 0**. Deliverables: complex COCG harmonic solver, amplitude/phase-lag fields, superposed instantaneous field, day/year time scrubber, plus the off-script `soil_rod` preset and a foldable line-probe profile. Operator authorized close. **To seal: commit on `main` + tag `m2-pass`** (commit made locally this session; `git push origin main` is blocked for the agent — Operator pushes). |
| 2026-06-16 | _working tree_ | G3.1 (auto) | PASS | Basement domain independence. "Domain ≥ 3·δ_annual" is a **periodic** statement, so the δ-governed quantities are asserted < 1 % on doubling the soil box (soilPad+soilDepth 9→18 m, grid 32×34×2 → 54×56×2): steady inner-surface mean T **0.200 %** (18.228 °C), **annual** inner-surface amplitude **0.052 %** (1.178 K), annual inner-surface flux **0.080 %** (6.288 W). Steady inner T 18.228 °C. **Scope note (read this):** the steady *total* flux to the deep T_mean Dirichlet is genuinely long-range — a fixed-temperature sink, not a far field — and converges slowly with depth (full 9→18 doubling moves it ~2.5 %); this is physics, not under-sizing. Its lateral far-field is adequate: steady Φ moves **0.263 %** on lateral-only doubling (soilPad 9→18, depth fixed). Gate therefore certifies the domain on the lateral steady flux + the three δ-governed quantities. |
| 2026-06-16 | _working tree_ | G3.2 (auto half) | PASS | Basement annual cycle, out of the model (default grid 48×50×2, δ_soil 2.835 m). Vertical soil column at mid-x = 5.30 m (pure soil): phase **lag over 2.95 m = 55.4 d** vs x/δ prediction 60.5 d (−8.4 %, < 15 %), inside the ~2-month window [45, 70]. Lag grows / amplitude decays monotonically downward (no anticipation). Instantaneous-field peak timing **from the model**: surface annual max **Jul 17**, max at 3 m depth **Sep 10** (54-day lag ≈ 1.8 months) — the cellar peaks in September. Human sign-off (line probe readout) pending below. |
| 2026-06-16 | _working tree_ | G3.3 (auto sanity) | PASS | `slab_junction` (concrete floor slab penetrating & interrupting exterior eps, flush to the exterior face): ψ = **1.0036 W/(m·K), external-dimension convention**, U 0.2686, L₂D 1.5407, f_Rsi(R_si=0.25) 0.682 (< 0.70 → mold flag at the slab — the classic cold-bridge). Sane/finite, |ψ| < 1.5. This is an aggressive fully-exposed-slab detail; ±15 % catalogue plausibility is the Operator's human sign-off (compare to an uninsulated penetrating-slab / balcony detail). |
| 2026-06-16 | _working tree_ | G3.4 (auto) | PASS | ω=0 collapse: `solveHarmonic(ω=0, dirichlet:'value')` reproduces `solveSteady` to < 1e-8 (relative, Re field) with Im < 1e-8 — on `wall1d` (Robin-only) and a coarse `basement` (Robin + Dirichlet). The harmonic operator at ω=0 is exactly the M1 real operator (re/im decoupled, bIm=0). |
| 2026-06-18 | _working tree_ | G3.2 + G3.3 (human) | PASS (delegated) | Operator authorized the seal on the agent's model-derived validation (he was low on bandwidth; not a hands-on UI pass this session). **G3.2:** the September story comes out of the model — annual surface max **Jul 17** → max at 3 m depth **Sep 10** (54-day lag ≈ 1.8 months); amplitude decays e^{−y/δ}, lag monotone with depth in the resolved band. **G3.3:** `slab_junction` ψ = **1.0036 W/(m·K), external dims** — physically correct for the modeled flush, fully-exposed penetrating slab (balcony-class), high vs a typical interrupted-insulation intermediate floor (BACKLOG: option to recess for ~0.5–0.7). UX rough edges (manual Solve, context-irrelevant sliders, no field-colors on the 3D model, 2D-extruded only) flagged by the Operator → parked in BACKLOG for the M4 interaction milestone. |
| 2026-06-18 | _working tree_ | **M3 complete** | PASS | All M3 gates green (G3.1–G3.4 auto; G3.2/G3.3 human accepted above). `node --test`: **77/77**. Deliverables: `slab_junction` + `basement` presets, ψ readout (external-dimension convention), f_Rsi report, per-frequency toggles. **To seal: tag `m3-pass`** (commit local; `git push origin main` blocked for the agent — Operator pushes). Next: M4 interaction & polish — auto-resolve sliders, field colors on the 3D model, volumetric 3D basement (see BACKLOG). |
| 2026-06-16 | _working tree_ | M3 `node --test` | — | **tests 77, pass 77, fail 0, skipped 0, todo 0**. Adds G3.1–G3.4 plus the two new presets. New presets `slab_junction` (psiSpec, external dims) and `basement` (room-void carve via an `air` box; BCs interior=`facesInside`, exterior=ground `{y,max}` climate, deep=`{y,min}` Dirichlet T_mean, cuts adiabatic) — both built with the **existing** BC selectors, no `fem.mjs` change. Per-frequency toggles (include annual/diurnal in the T(t) superposition) wired in `viz2d.mjs`/`ui.mjs`/`index.html`. M0 smoke test relaxed to allow a box painting the background id (void-carve). Browser-verified: basement f_Rsi 0.219 / θ_si,min 11.40 °C / Φ_ext 8.757 W, line probe along y (depth); slab_junction ψ readout states "external dimensions"; diurnal toggle shifts T(t) by ≤ 0.94 K. |
| 2026-06-18 | _working tree_ | M4 `node --test` | — | **tests 86, pass 86, fail 0, skipped 0, todo 0**. Adds `colormap.test.mjs` (6: diverging/sequential endpoints, midpoint, clamp, integer-RGB), 2 in `lineprobe.test.mjs` (`lineProbeCSV` header/rows/`\|T̂\|`; zero-harmonic case), and 1 in `physics.test.mjs` (`timeLagRelative`: φ_ref=0 ≡ absolute; in-sync→0 d despite ~½-yr absolute; extra-1-rad inward lag). M4 added the DOM-free `colormap.mjs` + `timeLagRelative` in `physics.mjs`; the rest of the headless numerics (`grid/fem/solver`) are unchanged, so all 77 prior gates stand. |
| 2026-06-18 | _working tree_ | Phase-lag map readability (M4 fix) | PASS | The τ map was *correct but unintuitive* — both the new-user and the Operator bounced off it. Root cause (debugged in physics, numbers below): τ = −arg(T̂)/ω was **absolute** (from midnight Jan 1), so the exterior surface — perfectly in sync with the forcing — read **197.6 d** (annual) / **16 h** (diurnal), not 0; and where the interior is held constant, deep \|T̂\|→0 nodes (>½ the diurnal corner2d domain) carried wrapping phase noise that blew out the 0–365 scale. Fix: new `timeLagRelative(re,im,ω,φ_ref)` referenced to the **outdoor forcing** phasor (worker now ships per-harmonic `refRe/refIm` from the exterior Robin region), plus a low-amplitude mask (\|T̂\| < 2 %·peak → NaN → shows material/voxel, no contour). **Verified live:** corner2d annual surface **0 d**, inner corner **2.05 d**, range 0–2.07 d (was 0–365); diurnal surface **0 h**, range 0–7.17 h, dead interior masked; basement annual surface **0.4 d** → **185 d** at depth (real deep-soil ½-yr lag, monotone), 1590 dead nodes masked. Affects only the phase **map**; amplitude / T(t) / line-probe unchanged. |
| 2026-06-18 | _working tree_ | G4.1 (auto/measured half) | PASS | **Scrub ≥ 30 fps:** 60-tick year sweep on `corner2d` (6727 cells, the heaviest 2D preset) = **12.6 ms/tick → ~79 fps**, including the field superposition + the 256×256 in-scene-plane texture refill + 2D panel + line-probe redraw + WebGL render. Pure phasor evaluation — never re-solves (DESIGN §3.5). **Slider→field < 5 s:** isolated full re-solve (steady + R_si=0.25 + annual + diurnal) wall1d **47 ms** / corner2d **946 ms** / basement **1888 ms** — all well under 5 s. **No UI freeze:** solve runs in the Worker; the insulation slider repaints geometry live on `onChange` and fires exactly one solve on `onFinishChange` (release). Auto-resolve correctness: thinning wall1d eps 0.16→0.05 m drops f_Rsi 0.952→0.880 (monotone). Visual eyeball of the breathing field on the 3D model + the 15-min test (G4.2) reserved for the Operator. |
| 2026-06-18 | _working tree_ | M4 UI (field on 3D, exports, polish) | — | **In-scene field slice plane** (DESIGN §6 headline): `Viz3D.setFieldSlice` paints an XY `DataTexture` plane mirroring the 2D panel's exact field/range/colormap (shared `colormap.mjs`); driven by a `SlicePanel.onFieldUpdate` hook so the scrubber breathes the field onto the 3D model — verified live (plane present, texture updates per tick, posZ tracks slice). **Auto re-solve:** Solve button **retired** (Operator decision); geometry changes solve automatically (Worker). **PNG export** of all three panels (slice 5.6 KB / probe 21 KB / 3D-WebGL 15 KB — `toBlob` works despite the full-page-screenshot WebGL hang) + **CSV** of the line probe (23 rows, header + per-harmonic Re/Im/\|T̂\|). **Control polish:** Insulation hidden on presets that ignore it (basement/slab/soil), shown on wall1d/corner2d; Slice-z hidden when geometry is z-uniform (all current presets). 3 new affordances (PNG, CSV buttons) — under Yuki's ≤ 3, and the retired Solve freed a slot. |
| 2026-06-18 | _working tree_ | G4.1 + G4.2 (human) | PASS | Operator sign-off. **G4.1:** scrub is smooth (~79 fps measured), slider→field re-solves in < 2 s on every default preset, no UI freeze — "works, produces nice animations, fast in the browser; does all that." **G4.2:** a cold user (non-physicist) produced a colored corner on the 3D model unaided; they didn't read f_Rsi (not a building physicist) — acceptable, not the goal. The one comprehension snag (phase-lag map) was traced to the absolute-reference + low-amplitude-noise issue and **fixed this session** (relative-to-outdoor + mask; row above) before sealing. Operator: "Good stuff." |
| 2026-06-18 | _working tree_ | **M4 complete** | PASS | All M4 deliverables shipped + signed off. `node --test`: **86/86**. Deliverables: in-scene field slice plane painted on the 3D model (breathes with the scrubber), auto re-solve (Solve button retired), day/year scrub (verified), PNG export of every panel + the 3D view, CSV export of the line probe, per-preset control hiding, and the phase-lag readability fix (relative-to-outdoor + low-amplitude mask). Parked in BACKLOG: volumetric 3D basement (capstone), play/pause continuous scrub (Operator QoL). **To seal: commit on `main` + tag `m4-pass`** (committed locally this session; `git push origin main` blocked for the agent — Operator pushes). |
| 2026-06-18 | _working tree_ | **M5 — `basement3d` preset (volumetric 3D cellar)** | PASS (auto) | M5 stretch item (Operator's showcase ask); M5 is a menu, this is the one item this session. New preset `basement3d` in `model.mjs`: a genuinely volumetric cellar on **quarter symmetry** (x=0, z=0 symmetry planes), so the quarter domain holds exactly one vertical wall–wall corner + the floor–wall–wall trihedral corner — the 3D geometric bridge the 2D-extruded `basement` can't show — at ¼ the grid. Same painter + BC selectors as 2D `basement` (`facesInside` room void, `{y,max}` ground climate, `{y,min}` deep Dirichlet T_mean, `'rest'` = symmetry planes + far soil). No `fem.mjs`/grid change. 3 new gate tests (`test/m5-basement3d.test.mjs`, patterns from G3.1/G3.2): **(1) lateral domain independence** — doubling soilPad 9→18 (x & z, the new 3D dimension) moves annual inner-surface amplitude, annual inner flux, and steady inner Φ all **< 1 %**; **(2) 3D corner cold-spot** — on the inner +x wall face the wall–wall-corner column is **colder than the symmetry-plane (2D-limit) column** (the 3D bridge concentrates loss); **(3) September story persists** — pure-soil column lags **≈2 months at ~3 m**, monotone with depth (no anticipation), in the true 3D domain. UI wiring **free**: appears in the dropdown; first preset to make `geometryVariesInZ` true → the dormant **Slice-z control auto-appears**; the M4 in-scene field plane finally paints a real 3D body. 0 new controls. |
| 2026-06-18 | _working tree_ | M5 `basement3d` perf + budget decision | NOTE | Isolated re-solve (steady + R_si=0.25 + annual + diurnal), measured outside `node --test` (handoff convention). Fine grid **maxH 0.5 → 30×32×30 = 31.7 k nodes: 18.2 s, OVER the 5 s budget** (steady ground-loss solve dominates at ~11.5 s / 287 it — long-range deep-Dirichlet sink, the slow-convergence behaviour G3.1 flagged). Readouts converge by maxH≈0.8 (f_Rsi 0.167, Φ_ext 198.9 W). **Decision (per handoff plan):** set an honest interactive **default maxH 0.8** (23×24×23 = 14.4 k nodes, **4.7 s — PASS**) and do **NOT** chain the matvec port — that is M5 item #2 (DESIGN §3.5 "only if a real use case exceeds the budget"), a separate session, now with `basement3d`'s fine grid as its documented motivating use case (→ BACKLOG). Browser-verified: dropdown entry, Slice-z appears, solve 12696 cells, f_Rsi 0.167 / θ_si,min 10.83 °C / Φ_ext 198.9 W (ψ n/a), field plane (247×256 over the 11.3×11.7 m domain) breathes with the year scrubber, 3D WebGL renders (8.8 KB `toBlob`), no console errors. |
| 2026-06-18 | _working tree_ | M5 `node --test` | — | **tests 89, pass 89, fail 0, skipped 0, todo 0** (86 → 89: +3 `basement3d` gates). Numerics (`grid/fem/solver/physics`) untouched — only `model.mjs` gains the preset — so all 86 prior gates stand. Formal human sign-off (eyeball the 3D cellar / corner) + the `m5-*` tag reserved for the Operator. |
| 2026-06-19 | _working tree_ | Polish D1 — stable T(t) color scale | PASS (auto) | The slice panel re-fit its color range to the *instantaneous* field every scrub tick, so a fixed-temperature node changed colour over time (the scale moved, not the physics). Fix: T(t) mode now locks the range to the field's full temporal **envelope** `T̄ ± Σ_enabled|T̂_k|` over solid nodes, computed once per solve (and on a frequency toggle), never on a scrub tick; amplitude/phase modes were already time-invariant. New pure `physics.instantRange(mean, harmonics)` (guaranteed time-invariant container of `phasorEval`); `viz2d` caches it (`_computeInstantRange`), 3D field plane inherits via `onFieldUpdate`. **Gate:** `physics.test.mjs` — `phasorEval ∈ instantRange` for many t across several periods, exact single-harmonic bound. **Browser:** basement3d displayed range identical at day ~15 / ~200 / ~290 (`[-3.85, 22.22]` all three). |
| 2026-06-19 | _working tree_ | Polish D2 — play/pause scrubber | PASS (auto) + human-gate reserved | A ▶/⏸ button animates time hands-free over the existing solve-free phasor path (DESIGN §3.5 — never re-solves); one sweep = 12 s; pause stops, grabbing the scrubber interrupts. Timing arithmetic in pure `src/scrub.mjs` (`advanceFraction` wraps into [0,1)). **Gate:** `scrub.test.mjs` — advance/wrap/loop correctness. **Browser:** button toggles play state; one tick's advance+applyScrub moves the fraction and breathes the field. The smooth-animation *heartbeat* is a G4.1-style **Operator human gate** — rAF is paused while a tab is backgrounded (`visibilityState hidden`), so it can't be observed from the headless preview. |
| 2026-06-19 | _working tree_ | Polish D3 — full-cellar symmetry mirror | PASS (auto) + human-gate reserved | `basement3d` solves a quarter (adiabatic planes x=0/z=0); the viewer now reflects it out to the whole cellar for **display only** — no re-solve. Preset declares `symmetry:['x','z']`; `viz3d` renders one voxel group per reflection (identity + 3 mirrors) and clones the field plane across the same transforms (clones share geometry+material+texture → one texture refill breathes every copy); camera frames the full mirrored extent. New pure `src/symmetry.mjs` (`symmetryTransforms`, `mirroredExtent`). **Gates:** `symmetry.test.mjs` — 2ⁿ copies, \|det\|=1, copies tile the full extent exactly (Σ volumes == total, union bbox == mirrored extent); `model.test.mjs` — **any preset declaring `symmetry` must have those planes adiabatic** (no Robin/Dirichlet selects the axis-min face; adiabatic `'rest'` exists) so the picture can't mirror a non-symmetric domain. **Browser:** basement3d → 4 voxel groups + 3 field mirrors spanning x,z ∈ [−10.93, 10.93], camera centered on the full model, renders; corner2d unchanged (1 group). Seam continuity / look = Operator human gate (WebGL not screenshot-able here). |
| 2026-06-19 | _working tree_ | Polish `node --test` | — | **tests 94, pass 94, fail 0, skipped 0, todo 0** (89 → 94: +1 `instantRange`, +1 `advanceFraction`, +2 `symmetry`, +1 symmetry↔adiabatic model gate). Headless numerics (`grid/fem/solver`) untouched; the polish lives in `physics`/`viz2d`/`viz3d`/`scrub`/`symmetry`/`index.html`, so all 89 prior gates stand. Three deliverables committed separately on `main` (`1e3fdf8` colors, `b2faa82` play, `d54c269` mirror). Human gates (play smoothness, mirror look) + tags reserved for the Operator. |
| 2026-06-18 | _working tree_ | M3 agent self-validation (Operator cooked) | NOTE — **not a human sign-off** | At the Operator's request, validated features + human-gate physics myself; formal G3.2/G3.3 sign-off + `m3-pass` tag still reserved for the Operator. **Features:** all 5 presets solve in-browser with sane readouts (wall1d f_Rsi 0.952, corner2d ψ −0.0646, slab_junction ψ 1.0036 / f_Rsi 0.682, basement f_Rsi 0.219 / Φ_ext 8.757 W, soil_rod steady Φ=0 as expected — uniform 9 °C); scrubber drives the annual breath (mid-Jan field −0.95 °C ground … mid-Jul 20.2 °C); amplitude maps annual 9.7 K / diurnal 3.2 K (surface); per-frequency toggle removes the diurnal swing. **G3.2 (physics PASS, eyeball pending):** mid-x soil column shows clean e^{−y/δ} decay (9.7→3.0→0.9 K at 0/3/6 m) and monotone-increasing lag in the physical band (199→253→314 d at 0/3/6 m); **annual peak surface Jul 17 → 3 m depth Sep 10 (54-day lag ≈ 1.8 months) — the cellar peaks in September, from the model.** **G3.3 (CONCERN — Operator's catalogue call):** ψ 1.0036 is physically correct for the modeled detail (slab flush to the exterior face, fully exposed, no break ≈ uninsulated-balcony class, published ~0.6–1.0) but ~40–100 % above a typical *intermediate-floor interrupted-insulation* figure (~0.5–0.7); whether it's within ±15 % depends on which anchor was chosen (DESIGN G3.3: "investigate, don't panic"). **Found:** phase-lag colormap saturates 0–365 d on basement (deep |T̂|→0 wrapping) → BACKLOG (M4); line probe unaffected. |
| 2026-06-19 | _working tree_ | **S2-M1.1 flux field** (ROADMAP Stage 2) | PASS (auto) | New `src/flux.mjs` — cell-centre heat-flux density q=−λ∇T: `cellFlux` (real), `cellFluxComplex` (phasor q̂=−λ∇T̂, separate re/im per CLAUDE.md M2), and the envelope integral `regionFlux`. Recovery = trilinear gradient at the cell centre, which collapses to a face-mean difference per axis ((mean +face − mean −face)/h) — **exact for linear fields**. Gates (`test/flux.test.mjs`, written gate-first): **G1.1a** linear field → q exact (~1e-12) on graded multi-material `corner2d`; **G1.1b** `wall1d` through-flux uniform / divergence-free to <1e-6, transverse <1e-9 (purely 1D); **G1.1d** independent-oracle cross-check — `regionFlux` == `boundaryFlux` per Robin region on the exact-solution `wall1d` to rel <1e-7 (two independent flux computations agree); **G1.1e** complex path: zero-im ≡ real `cellFlux`, linear phasor exact on re & im. One **additive** `fem.mjs` line (`face.cell`) so `regionFlux` can find each Robin face's owning cell — cannot affect existing behaviour. |
| 2026-06-19 | _working tree_ | S2-M1.1 `node --test` | — | **tests 99, pass 99, fail 0, skipped 0, todo 0** (95 → 99: +4 flux gates G1.1a/b/d/e). Headless numerics (`grid/solver/physics`) untouched; `fem.mjs` gains only the additive `face.cell` field, so all prior gates stand. Next S2-M1 subgoals: heat-flow viz (2D |q| colormap + glyphs, S2-M1.2) and `tools/proof.mjs`. |
| 2026-06-19 | _working tree_ | **Process guardrails** (operator robustness #3/#4/#5) | PASS (auto + dogfooded) | Baked the Operator's review checks into enforced machinery. **#5 golden numbers** — new `test/golden.test.mjs` pins live steady readouts (wall1d f_Rsi 0.9522 / Φ 0.0215; corner2d f_Rsi 0.8970 / ψ −0.06461 / Φ 5.3768; slab_junction f_Rsi 0.6824 / ψ 1.00355 / Φ 3.3896; basement f_Rsi 0.21853 / Φ 8.7567) at rel 1e-6, so a "no-op" change that moves physics fails the suite. **#4 test discipline + #3 tolerance tripwire** — new `tools/guardrails.mjs`: runs `node --test` and fails on fail/skipped/todo > 0 or test count < baseline (103); then diffs `test/`+`src/` vs HEAD and blocks any **removed/changed** tolerance line (loosening) unless `GUARDRAILS_ALLOW_TOL_CHANGE=1` (Operator sign-off only). Wired as `.githooks/pre-commit` (`git config core.hooksPath .githooks`); rule added to CLAUDE.md invariants. **Dogfooded:** loosening flux G1.1d 1e-7→1e-3 made the tripwire FAIL (exit 1, named the line); revert → green. `node --test` **103 pass, 0 fail** (99 → 103: +4 golden gates). |
| 2026-06-22 | _working tree_ | S2-G1.2a/b (auto) | PASS | Heat-flow viz gates, written gate-first. **G1.2a**: new `flux` heat ramp in `colormap.mjs` (dark slate → amber `#e8a33d` → pale) — endpoints / clamp / interior-stop / integer-RGB plus **monotone luminance** (so "brighter ≡ more flux", which the pixel proof relies on). **G1.2b**: pure `fluxGlyphs(q, grid, k, {stride, scale})` in `flux.mjs` — glyph **count == stride sampling** of solid cells, **direction == normalized in-plane (qx,qy) = −∇T**, **length == clamped \|q\|/scale**, void / zero-flux cells → no glyph, explicit-scale clamp. `test/colormap.test.mjs` +3, `test/flux.test.mjs` +2. |
| 2026-06-22 | _working tree_ | S2-G1.2c (agent-visual, `proof.mjs`) | PASS | New `tools/proof.mjs` — the Stage-2 headless **proof harness** (the verification spine): a capture snippet drives `window.__phasor` to a fixed state, reads the 2D `<canvas>` pixels + scrapes readouts; node-side pixel assertions + a proof-sheet writer. Drove `corner2d` to the steady heat-flow field and asserted on the canvas: **concentration** maxLum 232.2 − meanLum 107.2 = **125** (≥ 30); **bridge brighter than interior** corner-block 140.9 − interior 80.7 = **60.2** (≥ 15); **glyph layer non-empty** arrows change **21.1 %** of solid pixels (≥ 1 %). Sheet at `proofs/s2-m1.2/` (hero PNG + readouts + checks). Field readouts unchanged (corner2d f_Rsi 0.897 / ψ −0.0646 — golden). Agent-verified the hero PNG by eye: pale \|q\| band wraps the re-entrant corner, arrows lengthen there (length ∝ \|q\|). S2-H1.2 (Operator glance + 3D-WebGL) reserved. |
| 2026-06-22 | _working tree_ | S2-M1.2 viz (Heat flow \|q\|) | — | New SlicePanel field mode `'flux'` ("Heat flow \|q\|"): client-side `cellLambda` from the model → `cellFlux` of the instantaneous T(t) on the **solve-free scrub path** (never re-solves, DESIGN §3.5); per-cell \|q\| painted cell-wise with the heat ramp. **Fixed** colour scale = 99th-percentile of the **steady** in-plane \|q\| — robust to the re-entrant-corner flux singularity (the bulk fills the ramp, the bridge saturates), and stable across scrub so the field brightens in winter / quiets in summer without the scale moving. Vector-glyph overlay (`fluxGlyphs`, dark-halo + light-core arrows, ~14 across) with a "Flow arrows" toggle; the 3D plane keeps showing T(t) (emit override). `viz2d.mjs` / `colormap.mjs` / `flux.mjs` / `ui.mjs` / `index.html`; no `grid/fem/solver` change. |
| 2026-06-22 | _working tree_ | S2-M1.2 `node --test` | — | **tests 108, pass 108, fail 0, skipped 0, todo 0** (103 → 108: +3 colormap `flux`, +2 `fluxGlyphs`). `MIN_TESTS` 103→108. **Env note (not a regression):** the G2.5 48³ wall-clock gate (< 10 s) sits right at its budget under current machine load — 9.96 / 10.02 / 10.25 s across runs — so it flakes in the full parallel suite; the solver is untouched by this change and it passes in lighter runs. (Committed `a7bdac4`.) |
| 2026-06-22 | _working tree_ | S2-G1.3 (auto) | PASS | Harmonic-only view rests on the ω>0 field being **purely AC** — no steady/DC leaking into the drive. "It goes flat", as a number: the **diurnal** swing dies in a heavy wall, so interior surface \|T̂\| ≪ the outdoor peak. `wall1d` interior/peak = **2.2e-3**, `corner2d` **4.1e-3** — both gated < **1 %** (a DC leak into the harmonic load would floor the interior to ~order-1, ~100× over the bar). `test/harmonic-only.test.mjs` (+2). Basement is **not** flat (room sits shallow enough that the wave reaches it, ratio ~0.7–0.8) — a separate story, out of M1.3 scope. |
| 2026-06-22 | _working tree_ | S2-M1.2c+M1.3 visual (`proof.mjs`) | PASS | Sheets in `proofs/s2-m1.2/` (heat-flow) and `proofs/s2-m1.3/` (harmonic swing). **M1.3 pixel checks:** sign-flip — winter field **39.1 % cool / 0 % warm** ↔ summer **39.9 % warm / 0 % cool** (the AC deviation flips with the scrubber); DC-removed — **60.9 % neutral** (Δθ≈0) where the wall damps the wave (the flat interior). New harness assertion `assertSignFlip`. Hero PNG agent-verified: exterior edge swings deep blue (Δθ<0), damping through grey to the flat interior. |
| 2026-06-22 | _working tree_ | S2-M1.3 viz (Harmonic swing Δθ(t)) | — | New SlicePanel mode `'swing'` ("Harmonic swing Δθ(t)"): single selected-frequency AC deviation **Δθ(t) = Re[T̂_ω e^{iωt}]**, steady field removed, on the **solve-free scrub path**. Diverging map centred on 0; **fixed symmetric scale ±\|T̂\|_max** (time-invariant, so only the pattern moves). Breathes with the scrubber and flips sign winter↔summer; the damped interior reads flat grey (DC gone). `viz2d.mjs` + `ui.mjs` (FIELD_MODES, freq label); no `index.html`/`grid/fem/solver` change (reuses the freq selector + scrubber). |
| 2026-06-22 | _working tree_ | S2-M1.3 `node --test` | — | **tests 110, pass 110, fail 0, skipped 0, todo 0** (108 → 110: +2 S2-G1.3). `MIN_TESTS` 108→110. Headless numerics unchanged; the view is DOM-only (`viz2d`/`ui`). S2-H1.2 (Operator glance at both proof sheets + 3D-WebGL) still reserved. (Committed `e21819c`.) |
| 2026-06-22 | _working tree_ | S2-G1.4a/b (auto) | PASS | Annual heat-loss curve by **superposition** (never a re-solve). **G1.4a:** the reconstructed curve (per-harmonic region-flux phasors superposed, `lossCurveSamples`) equals the direct route (instantaneous nodal field → `cellFlux` → `regionFlux`) on `basement` to **rel < 1e-7** across 24 samples — two independent routes to Φ(t). **G1.4b:** the air case (`basement_air`) solves and its annual-mean interior loss **40.85 W > 9.98 W** earth (soil insulates + damps). New pure `src/losscurve.mjs` (`regionFluxReal`, `regionFluxPhasor`, `lossCurveSamples`, `lossAt`) — thin linear composition over flux.mjs. `test/losscurve.test.mjs` (+2). |
| 2026-06-22 | _working tree_ | S2-M1.4 `basement_air` + region-flux numbers | — | New sibling preset **`basement_air`** (same cellar wall+floor, soil removed, outer faces on the air film R_se=0.04 via `'rest'`-after-symmetry; room inner faces stay `facesInside` — same selector trick, no new machinery). Verified faces by region areas (interior 0.9 m² **identical** to `basement` → fair comparison; exterior 3.48 m²). Earth interior loss Φ̄ **−9.98 W**, annual flux amplitude **5.69 W** (damped); air Φ̄ **−40.85 W**, amplitude **37.1 W** (undamped) → **earth/air = 0.24** (soil cuts annual-mean loss to 24 %). |
| 2026-06-22 | _working tree_ | S2-M1.4 viz (Annual heat-loss panel) | — | New 3rd foldable panel `LossView` (`src/lossview.mjs`, DOM; `losscurve.mjs` stays DOM-free for the worker / `node --test`). Plots Φ(t) over the year, **earth vs air with the gap shaded** (chart-with-gap idiom), live scrubber marker, readout `earth 10.0 W · air 40.8 W · earth/air 0.24`. Worker (`worker.mjs`) computes the interior region-flux phasors **before transferring** the Tre/Tim buffers and ships `loss:{earth,air}` (air only for `basement`, via the new `computeLoss`). Non-basement presets show a single curve; `soil_rod` (no interior) shows "solve to populate". Verified live: no console errors; corner2d/wall1d single-curve, soil_rod empty. |
| 2026-06-22 | _working tree_ | S2-G1.4 visual (`proof.mjs`) | PASS | Sheet `proofs/s2-m1.4/`. Pixel checks on the rendered chart: both curves drawn (earth amber 383 px + air red 1909 px), reduction factor 0.244 < 1, air swing 93 W ≫ earth swing 11.9 W. Agent-verified the PNG by eye: air (red, dashed) swings 0→93 W over the year with the diurnal ripple; earth (amber) low/damped/lagged; the two cross in summer (the ~2-month soil lag). |
| 2026-06-22 | _working tree_ | S2-M1.4 `node --test` | — | **tests 112, pass 112, fail 0, skipped 0, todo 0** (110 → 112: +2 S2-G1.4). `MIN_TESTS` 110→112. `grid/fem/solver/physics` untouched; new code in `losscurve` (pure, gated), `worker` (additive loss payload + `basement_air` air-case solve), `model` (+preset), `lossview`/`index.html` (DOM). |
| 2026-06-22 | _working tree_ | S2-G1.5 (auto, calibration) | PASS | **M1.5 part 1** — `src/standards.mjs` (DIN EN ISO 13370 + 12831 closed forms). **Finding:** the 2018 standard carries **no worked numeric example** (companion ISO/TR docs hold them), so the gate anchors on its **published** numbers + stated identities (non-circular): **G1.5a** periodic penetration depth (Eq H.1) reproduces **Table H.1** δ = 2.2/3.2/4.2 m from the Table 7 ground properties (computed 2.239/3.166/4.189 → round to table within the 0.1 m grid); **G1.5b** heated-basement floor U (Eq 13/14) reduces to slab-on-ground U (clause 7.1) at z=0 (7.3.1 NOTE 1) to < 1e-12; **G1.5c** equivalent thickness / wall U / H_g / 12831 load sane + signed. `test/standards.test.mjs` (+3). `node --test` **115/115**, `MIN_TESTS` 112→115. Committed `12eb0a9`. **IP:** only equations + published reference values in code; norm PDFs stay gitignored. |
| 2026-06-22 | _working tree_ | S2-M1.5 13370-vs-PHASOR scoping (NOTE) | — | Worked the comparison for `basement` (2D strip: B'=4 m full width, z=2.5 m, bare concrete, soil λ=2.0 = Table 7 sand/gravel): U_fg,b 0.533, U_wg,b 0.901 W/m²K → **13370 ≈ 36.5 W/m vs PHASOR ≈ 49.9 W/m (ratio 0.73, ~27 %)**. Same order + sign, but loose — reconciling it **is** the steady-baseline-pinning task (decision 2; PHASOR's finite domain + deep-Dirichlet sink + surface-driven BC vs 13370's equivalent-thickness idealization). **Part 2 (comparison panel + pinning) deferred** to a focused session — and entangled with the Operator's flagged viz rework (BACKLOG 2026-06-22), so the comparison *view* should fold into that rather than be built standalone. |
| 2026-06-23 | _working tree_ | view-rework gates | PASS | New `test/slice.test.mjs` (4: nodal bilinear exact for a linear field on **all 3 axes**, cell sampler picks the nearest cell, void→NaN) + **axis-generalized `fluxGlyphs`** (extended G1.2b: x/y/z select the right in-plane components + cell layer). `node --test` **120/120**, `MIN_TESTS` 115→120. Pure helpers gated; the WebGL rendering itself is the human channel. |
| 2026-06-23 | _working tree_ | 3D view rework — unified cutting plane | — | Operator flag: the in-scene field was a `depthTest:false` overlay **floating** over the model, and the geometry clip was a **separate unrelated axis**. Reworked to **one selectable-axis (x/y/z) cutting plane** that clips the object AND carries the field on the **embedded** cut face (`depthTest:true` + polygon-offset). Architecture: `viz2d` emits the displayed field as full-grid arrays `{kind,field,range,cm,fluxQ}` (flux ships per-cell \|q\| + flux vectors, **dropping the temperature emit-override**); `viz3d` owns the cut, samples any plane via the new pure `src/slice.mjs`, draws flux arrows from the axis-generalized `fluxGlyphs`, mirrors plane+arrows for symmetry presets. Controls unified: **Cut axis/position** replaces the separate Clip; the 2D z-slice syncs when cutting along z. **Verified live** (toBlob WebGL capture + scene diagnostics): corner2d z-cut (field+arrows on the embedded cut), x-cut (mesh at x=0.71, a=y/b=z, pale flux colors, 84 arrows), basement3d z-cut (field on cut + **4 symmetry quadrants**), nodal T(t) (no arrows, diverging) + flux \|q\| paths, no console errors. Final aesthetic sign-off = Operator (WebGL human channel). |
